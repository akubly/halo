# Orchestration Log: Juanita (Tester / QA)

**Date:** 2026-06-08T00:48:54Z  
**Agent:** Juanita  
**Task:** Test Strategy Rev 2 — Review Findings Closed  
**Status:** COMPLETED

## Work Summary

Advisory rubber-duck review of TEST-STRATEGY.md Rev 1 surfaced 11 findings: 3 blocking, 7 important, 1 minor category. Root causes: ARD ambiguities (heap, quick-reset, confidence-gating), under-specified wire format, test false positives, mixed-methodology framing, acceptance test coupling to heuristic internals, story-mapping misalignment. Aaron approved Hiro + Ng to amend ARD and wire format; Juanita to revise strategy doc.

### Blocking Findings — Closed

**B1 — Heap Tests → Device Tier Only** ✅
- Heap management entirely device-local (Lua). No heap state on wire.
- `FAMILIAR_ACK` is seq-only (no heap flag).
- §6.3 rewritten as device-tier-only tests (busted + emulator); gated behind emulator API availability.
- JUANITA-T2-3 story mapping updated to "Device (Lua/emulator)" tier.

**B2 — Wire Format Under-Specified** ✅
- All `struct.unpack(">H", ...)` replaced with `<H` (little-endian, BLE ATT native, Cortex-M55 native).
- §6.6 rewritten with full signed-16 delta window dedup rule: delta 1–32767 = accept, 0 = drop duplicate, 32768–65535 = drop stale.
- Explicit tests added: duplicate (delta=0), stale (delta≥32768), 0xFFFF→0x0000 wraparound boundary.
- `FAMILIAR_RESET` = opcode 0x01, Device→Host, 1 byte, no payload (per Hiro decision).
- `FAMILIAR_ACK` = opcode 0x02, auto every 10 accepted packets, seq-only (uint16 LE, 3 bytes total per Ng decision).
- Protocol decode tests added for both `FAMILIAR_ACK` and `FAMILIAR_RESET`.

**B3 — False-Positive Bug in §4.2** ✅
- `test_low_confidence_frame_below_neutral_band_also_suppressed` was using two different `FakeTransport()` instances (passed to constructor vs. asserted on).
- Test could never fail on the right thing (instance mismatch).
- Fixed: Single `transport = FakeTransport()` used for both constructor argument and `assert transport.packet_count() == 0`.
- Code comment added documenting intent.

### Important Findings — Closed

**I4 — Honest London-School Framing** ✅
- §1 claimed entire strategy is mockist; Tell-Don't-Ask example incorrectly stated orchestration lives in `inference.py`.
- §4.1 Red test claimed "FamiliarApp doesn't exist" (NameError is not meaningful Red).
- Fix: §1 updated with explicit mixed-methodology table: acceptance = London/mockist; unit tests = deliberately classicist (pure functions, value objects, no collaborators to mock).
- Tell-Don't-Ask corrected: `FamiliarApp` reads from sensor source, calls `inference_fn`, tells transport to send. `inference.py` does not orchestrate.
- §4.1 Red guidance: pre-stub `FamiliarApp` as no-op class; Red fails because `transport.packet_count() == 0` (behavioral silence), not `NameError`.

**I5 — Decouple Acceptance Tests from Heuristic Internals** ✅
- Acceptance tests (§4.1/§4.2) were using real sensor values (audio_rms=0.8, etc.) coupled to `compute_mood` thresholds.
- All acceptance tests (§4.1, §4.2, §4.3, §6.4) now inject `inference_fn` returning controlled dict.
- Sensor values and confidence numbers stay in `tests/unit/test_inference.py` (classicist, pure-function tests).
- Seam: `FamiliarApp(transport, sensor_source, clock, inference_fn=compute_mood)`.

**I6 — Confidence Gating Ownership** ✅
- §7.2 busted test was asserting device-side gating as required behavior.
- Relabeled as "optional defense-in-depth only."
- Comment states: host is sole authority (ARD §5.4); device-side gating not required.
- Test validates no crash and no invalid state, not a gate requirement.

**I7 — Privacy/Jitter** ✅
- §6.8: Added seeded-RNG seam for animation jitter.
- Busted test with `math.randomseed(42)` asserts jitter stays within 5–10% per ARD §5.6.
- Open item tracked in Appendix A #5 (whether injectable RNG seam exists in production Lua).
- §4.1: Added `test_familiar_update_carries_no_raw_biometric_values` — asserts `encode_familiar_update` signature cannot accept raw sensor parameters.
- RAVEN-T2-1 story mapping updated: automated protocol assertion + manual visual audit.

**I8 — Lua Testing Authority** ✅
- §7.3 Python simulation was described as validating Lua behavior (Python-to-Python comparison has no connection to production Lua).
- §7.2 declares `busted` authoritative Lua unit check.
- §7.3 rewritten: property tests must drive real Lua interpreter (e.g. via `lupa` binding or subprocess), not Python clone.
- §7.3a retained as note on bounded usefulness of Python simulation as spec document only.
- §7.6: Added explicit table of what can ONLY be verified on real hardware.

**I9 — Quick-Reset Seam** ✅
- §5 JUANITA-T2-5 said "Double-tap FAMILIAR_RESET opcode is sent" (ambiguous origin).
- §7.5 said "double-tap FAMILIAR_RESET works" (vague).
- JUANITA-T2-5 mapping updated: "Device detects double-tap → Lua snaps to NEUTRAL locally; host receives FAMILIAR_RESET notification (opcode 0x01)."
- §7.2 busted test added for `sm:on_double_tap()` → local NEUTRAL snap → `sm:pending_notification_opcode() == 0x01`.
- §7.5 milestone check updated: "host sees FAMILIAR_RESET notification."
- No test constructs host-to-device reset message.

**I10 — Story-Mapping Alignment** ✅
- YT-T2-2 described "detects rising tension 2+ cycles before threshold" — predictive/leading-indicator behavior not in ARD (false feature claim).
- RAVEN-T2-1 was manual-only despite having automatable protocol assertion.
- YT-T2-2 replaced with: "Baseline adaptation: after accumulating history, `compute_mood` thresholds shift toward personal mean; confidence increases for familiar patterns." (ARD-grounded).
- RAVEN-T2-1 updated to "Acceptance (protocol) + Manual (visual)".
- §9 P0 story DoD reconciliation table added: documents RAVEN-T2-1 split (automated protocol + manual visual) and ENZO/DASID-T2-5 as explicit product-validation exceptions.

### Minor Finding — Closed

**M11 — Appendix A Blockers** ✅
- All 5 Appendix A items were open, including now-settled items (FAMILIAR_ACK cadence, opcode, endianness, heap, quick-reset).
- Restructured into two sections:
  - **RESOLVED (R1–R7):** endianness (LE), ACK trigger/payload (auto every 10, seq-only), seq wraparound/dedup (signed-16 delta), FAMILIAR_RESET direction/opcode (Device→Host, 0x01), heap ownership (device-local), confidence authority (host-only), quick-reset ownership (device-originated).
  - **OPEN (items 1–7):** emulator API availability (`get_framebuffer`, `set_heap_usage`, IMU injection), seedable RNG seam for animation jitter, canonical sprite pixel-buffer format, baseline-persistence location, IMU interrupt primitive.

## Files Modified

- `docs/projects/synesthetic-familiar/TEST-STRATEGY.md` (Rev 2)

## Decision Rationale

All 11 findings addressed via three-agent coordination:
1. **Hiro:** ARD clarity (heap device-local, quick-reset device-originated, confidence gating host-authority) → removes testing ambiguity.
2. **Ng:** Wire-format lock (LE, signed-16 dedup, opcode assignments, ACK cadence) → test code can no longer invent protocol details.
3. **Juanita:** Test strategy revision (fix false positives, correct framing, decouple tests, align stories, resolve all 11 findings) → buildable test suite.

Review-driven remediation chain ensures ARD, wire format, and test strategy converge by 2026-06-08.
